{
  "name": "trend-detect-reports-site",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:prepare": "node scripts/build-vitepress-site.mjs",
    "docs:build:site": "vitepress build site",
    "docs:dev": "npm run docs:prepare && vitepress dev site",
    "docs:build": "npm run docs:prepare && vitepress build site",
    "docs:preview": "npm run docs:build && vitepress preview site",
    "docs:preview:site": "vitepress preview site"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
